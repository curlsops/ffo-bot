apiVersion: v1
kind: Service
metadata:
  name: ffo-bot
  namespace: ffo-bot
  labels:
    app.kubernetes.io/name: ffo-bot
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: ffo-bot
